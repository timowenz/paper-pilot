# Copy to `docker.env` (or export in your shell) and use with docker compose.
# Example: docker compose --env-file docker.env -f docker-compose.ghcr.yml up

GHCR_OWNER=your-github-username
OPENROUTER_API_KEY=sk-or-v1-...
# COHERENCE_MODEL=openai/gpt-oss-20b:free
# COHERENCE_MAX_TOKENS=16384
